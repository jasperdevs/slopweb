{
  "name": "slopweb",
  "version": "1.0.2",
  "description": "A new web where AI generates every page.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node ../scripts/check.mjs ."
  },
  "engines": {
    "node": ">=18.17"
  }
}
